import React from 'react';

export default function Data({id}){
    return (

        <div id={id}>
            <table>
                <thead>
                    data
                </thead>
                <tbody>
                    data
                </tbody>
            </table>
        </div>

    )
}